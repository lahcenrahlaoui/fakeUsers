import { GoTriangleDown, GoX, GoSync } from "react-icons/go";

const UserHeader = ({
    user,
    isDeletingUser,
    handleDeleteUser,
    deletingUserError,
}) => {
    return (
     
                    
            <div></div>   
    );
};

export default UserHeader;
